{
  "name": "camden-snowden-portfolio",
  "version": "1.0.0",
  "description": "Camden Snowden's personal portfolio website",
  "scripts": {
    "start": "npx http-server .",
    "build": "echo 'Building site...' && mkdir -p dist && cp -r ./* dist/",
    "clean": "rm -rf node_modules dist",
    "optimize": "bash optimize_images.sh"
  },
  "dependencies": {
    "http-server": "^14.1.1"
  },
  "devDependencies": {},
  "engines": {
    "node": "18.x",
    "npm": "9.x"
  },
  "private": true,
  "license": "UNLICENSED"
} 